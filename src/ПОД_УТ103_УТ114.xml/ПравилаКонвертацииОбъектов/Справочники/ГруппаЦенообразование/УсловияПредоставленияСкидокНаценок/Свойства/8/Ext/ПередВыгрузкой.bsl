Если ТипЗнч(Источник.ЗначениеУсловия) = Тип("СправочникСсылка.ВидыОплатЧекаККМ") Тогда
	Значение = ОбщегоНазначения.ПолучитьЗначениеРеквизита(Источник.ЗначениеУсловия, "ТипОплаты");
	Если ЗначениеЗаполнено(Значение) Тогда
		ИмяПКО = "ФормыОплаты";
	Иначе
		Отказ = Истина;
	КонецЕсли;	
Иначе
	Отказ = Истина;
КонецЕсли;
